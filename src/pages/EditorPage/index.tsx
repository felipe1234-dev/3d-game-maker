import Viewport from "./Viewport";
import "@local/styles/pages/EditorPage.scss";

function EditorPage() {
    return (
        <>
            <Viewport />
        </>
    );
}

export default EditorPage;